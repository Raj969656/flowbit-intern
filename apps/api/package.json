{
  "name": "@flowbit/api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prebuild": "npm install typescript",
    "build": "SKIP_PRISMA_GENERATE=true npx tsc -p tsconfig.json",
    "start": "npx prisma generate --schema=../../prisma/schema.prisma && npx prisma migrate deploy --schema=../../prisma/schema.prisma && node dist/index.js",
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts"
  },
  "prisma": {
    "skipPostinstall": true
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.19.2",
    "prisma": "^5.22.0",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "cross-env": "^10.1.0"
  }
}
